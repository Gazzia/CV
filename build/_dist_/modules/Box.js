import './Box.css.proxy.js';
/* src\modules\Box.svelte generated by Svelte v3.29.4 */
import {
	SvelteComponent,
	add_render_callback,
	append,
	attr,
	create_in_transition,
	create_slot,
	detach,
	element,
	init,
	insert,
	safe_not_equal,
	set_data,
	space,
	text,
	toggle_class,
	transition_in,
	transition_out,
	update_slot
} from "../../web_modules/svelte/internal.js";

import { slide } from "../../web_modules/svelte/transition.js";

function create_fragment(ctx) {
	let section;
	let h3;
	let t0;
	let t1;
	let div;
	let section_intro;
	let current;
	const default_slot_template = /*#slots*/ ctx[3].default;
	const default_slot = create_slot(default_slot_template, ctx, /*$$scope*/ ctx[2], null);

	return {
		c() {
			section = element("section");
			h3 = element("h3");
			t0 = text(/*title*/ ctx[0]);
			t1 = space();
			div = element("div");
			if (default_slot) default_slot.c();
			attr(h3, "class", "title highlight svelte-8fntcu");
			attr(div, "class", "boxContent svelte-8fntcu");
			toggle_class(div, "dated", /*dated*/ ctx[1]);
			attr(section, "class", "svelte-8fntcu");
		},
		m(target, anchor) {
			insert(target, section, anchor);
			append(section, h3);
			append(h3, t0);
			append(section, t1);
			append(section, div);

			if (default_slot) {
				default_slot.m(div, null);
			}

			current = true;
		},
		p(ctx, [dirty]) {
			if (!current || dirty & /*title*/ 1) set_data(t0, /*title*/ ctx[0]);

			if (default_slot) {
				if (default_slot.p && dirty & /*$$scope*/ 4) {
					update_slot(default_slot, default_slot_template, ctx, /*$$scope*/ ctx[2], dirty, null, null);
				}
			}

			if (dirty & /*dated*/ 2) {
				toggle_class(div, "dated", /*dated*/ ctx[1]);
			}
		},
		i(local) {
			if (current) return;
			transition_in(default_slot, local);

			if (!section_intro) {
				add_render_callback(() => {
					section_intro = create_in_transition(section, slide, { delay: 200 });
					section_intro.start();
				});
			}

			current = true;
		},
		o(local) {
			transition_out(default_slot, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(section);
			if (default_slot) default_slot.d(detaching);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	let { title = "" } = $$props;
	let { dated = false } = $$props;

	$$self.$$set = $$props => {
		if ("title" in $$props) $$invalidate(0, title = $$props.title);
		if ("dated" in $$props) $$invalidate(1, dated = $$props.dated);
		if ("$$scope" in $$props) $$invalidate(2, $$scope = $$props.$$scope);
	};

	return [title, dated, $$scope, slots];
}

class Box extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { title: 0, dated: 1 });
	}
}

export default Box;