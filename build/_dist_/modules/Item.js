import './Item.css.proxy.js';
/* src\modules\Item.svelte generated by Svelte v3.29.4 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	space,
	text as text_1
} from "../../web_modules/svelte/internal.js";

function create_if_block(ctx) {
	let div;
	let t;

	return {
		c() {
			div = element("div");
			t = text_1(/*date*/ ctx[1]);
			attr(div, "class", "date svelte-w1sq3n");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, t);
		},
		p(ctx, dirty) {
			if (dirty & /*date*/ 2) set_data(t, /*date*/ ctx[1]);
		},
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

function create_fragment(ctx) {
	let span;
	let div;
	let t0;
	let t1;
	let if_block = /*date*/ ctx[1] && create_if_block(ctx);

	return {
		c() {
			span = element("span");
			div = element("div");
			t0 = text_1(/*text*/ ctx[0]);
			t1 = space();
			if (if_block) if_block.c();
			attr(div, "class", "text");
			attr(span, "class", "svelte-w1sq3n");
		},
		m(target, anchor) {
			insert(target, span, anchor);
			append(span, div);
			append(div, t0);
			append(span, t1);
			if (if_block) if_block.m(span, null);
		},
		p(ctx, [dirty]) {
			if (dirty & /*text*/ 1) set_data(t0, /*text*/ ctx[0]);

			if (/*date*/ ctx[1]) {
				if (if_block) {
					if_block.p(ctx, dirty);
				} else {
					if_block = create_if_block(ctx);
					if_block.c();
					if_block.m(span, null);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(span);
			if (if_block) if_block.d();
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { text = "" } = $$props;
	let { date = false } = $$props;

	$$self.$$set = $$props => {
		if ("text" in $$props) $$invalidate(0, text = $$props.text);
		if ("date" in $$props) $$invalidate(1, date = $$props.date);
	};

	return [text, date];
}

class Item extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { text: 0, date: 1 });
	}
}

export default Item;